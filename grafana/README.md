# This folder contains the configuration files for the grafana instance of the wildlens system.


## Minimum `.env.grafana` file template to start the service :

```env
- GF_SECURITY_ADMIN_PASSWORD
- GF_SECURITY_ADMIN_USER
```